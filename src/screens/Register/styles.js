import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    defaultInput: {
        width: "95%",
        backgroundColor: "#fff",
        margin: 10,
        borderRadius: 8,
    }
});

export default styles;